Prerequisites
-------------

dnf install \
  qemu-kvm \
  libvirt-devel \
  virt-install \
  python3-devel \
  python3-libvirt \
  python3-dns \
  python3-yaml

User configuration
------------------

Set permissions:

  sudo usermod -a -G kvm,qemu $USER
  sudo mkdir -p        /var/lib/libvirt/filesystems /var/lib/libvirt/images
  sudo chown qemu:qemu /var/lib/libvirt/filesystems /var/lib/libvirt/images
  sudo chmod og=rwx    /var/lib/libvirt/filesystems /var/lib/libvirt/images

Add to your bash profile:

  export LIBVIRT_DEFAULT_URI='qemu:///system'

Bridging:

  virsh net-define bridge.xml
  virsh net-start bridge
  virsh net-autostart bridge
  virsh net-list --all

Guest disk partition:

  yum install  cloud-utils-growpart
  growpart /dev/vda 1
  xfs_growfs /dev/vda1
